<% if @locked %>
  <div class="text-center mt-20">
    <h1>🚆 BrainTrain is closed right now</h1>
    <p>Come back during your commute hours (6–9 AM or 4–7 PM)</p>
  </div>
<% else %>
  <div class="max-w-md mx-auto text-center mt-20">
    <h1 class="text-3xl font-bold mb-4">🧠 Word Scramble</h1>
    <p class="text-lg mb-2">Unscramble the word:</p>
    <div class="text-2xl font-mono mb-4"><%= @scrambled %></div>

    <%= form_with url: braintrain_submit_path, local: true do |f| %>
      <%= f.hidden_field :correct_answer, value: @word %>
      <%= f.text_field :user_answer, placeholder: "Your answer", class: "border p-2 rounded w-full" %>
      <%= f.submit "Submit", class: "mt-3 bg-blue-600 text-white px-4 py-2 rounded" %>
    <% end %>

    <% if flash[:notice] %>
      <p class="mt-4 text-green-600"><%= flash[:notice] %></p>
    <% elsif flash[:alert] %>
      <p class="mt-4 text-red-600"><%= flash[:alert] %></p>
    <% end %>
  </div>
<% end %>